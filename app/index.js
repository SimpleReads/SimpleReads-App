import {render} from 'react-dom'; //PAckage which enables rendering of components and elements on  the web
import Web from './Web'
import './index.css' // Sets some order of elements


render (
    <Web />,
    document.getElementById('root')
)
